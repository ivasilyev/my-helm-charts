apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Values.mysql.app_name }}-service
spec:
  selector:
    app: {{ .Values.mysql.app_name }}
  ports:
    - protocol: {{ .Values.mysql.serverProtocol }}
      port: {{ .Values.mysql.dbPassword }}
